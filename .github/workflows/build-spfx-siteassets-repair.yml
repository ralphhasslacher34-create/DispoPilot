name: Build DispoPilot SITEASSETS Repair

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'

      - name: Check existing DispoPilot sources
        run: |
          test -f dispopilot-online-app.html
          test -f prepare-spfx-final.py
          test -f patch-dp-users-v2.py
          python3 -m py_compile prepare-spfx-final.py
          python3 -m py_compile patch-dp-users-v2.py
          grep -q "DispoPilot SPFx FINAL JSONC v1" prepare-spfx-final.py
          grep -q "DISPOPILOT_DP_USERS_AUTH_V2" patch-dp-users-v2.py

      - name: Create current authenticated app source
        run: |
          python3 patch-dp-users-v2.py \
            --input dispopilot-online-app.html \
            --output dispopilot-online-siteassets.html
          grep -q "DISPOPILOT_DP_USERS_AUTH_V2" dispopilot-online-siteassets.html
          grep -q "DP_Users" dispopilot-online-siteassets.html

      - name: Install SPFx CLI
        run: npm install -g @microsoft/spfx-cli@latest

      - name: Scaffold SPFx web part
        run: |
          rm -rf spfx-build
          spfx create \
            --template webpart-noframework \
            --library-name dispopilot-online-v13 \
            --component-name "DispoPilot Online" \
            --component-alias DispoPilotOnline \
            --component-description "DispoPilot Online V1.3" \
            --solution-name dispopilot-online-v13 \
            --target-dir spfx-build \
            --package-manager npm

      - name: Insert unchanged DispoPilot runtime
        run: |
          python3 prepare-spfx-final.py \
            --app dispopilot-online-siteassets.html \
            --project spfx-build

      - name: Configure SiteAssets hosting
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          # Keep the same solution/component IDs. Only the technical asset host changes.
          package = Path('spfx-build/config/package-solution.json')
          pdata = json.loads(package.read_text(encoding='utf-8'))
          solution = pdata['solution']
          solution['version'] = '1.3.5.1'  # technical repair revision only
          solution['includeClientSideAssets'] = False
          solution['skipFeatureDeployment'] = True
          package.write_text(json.dumps(pdata, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')

          write_manifests = Path('spfx-build/config/write-manifests.json')
          wm = {
            '$schema': 'https://developer.microsoft.com/json-schemas/spfx-build/write-manifests.schema.json',
            'cdnBasePath': 'https://ralphyourway.sharepoint.com/sites/DispoPilot/SiteAssets/DispoPilotAssets'
          }
          write_manifests.write_text(json.dumps(wm, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')

          manifests = list(Path('spfx-build/src/webparts').rglob('*.manifest.json'))
          if not manifests:
              raise SystemExit('Kein Webpart-Manifest gefunden.')
          for manifest in manifests:
              data = json.loads(manifest.read_text(encoding='utf-8'))
              hosts = data.setdefault('supportedHosts', [])
              for host in ('SharePointWebPart', 'SharePointFullPage'):
                  if host not in hosts:
                      hosts.append(host)
              manifest.write_text(json.dumps(data, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')

          print('SiteAssets URL:', wm['cdnBasePath'])
          print('includeClientSideAssets:', solution['includeClientSideAssets'])
          print('technical package revision:', solution['version'])
          PY

      - name: Verify SiteAssets configuration
        run: |
          grep -q '"includeClientSideAssets": false' spfx-build/config/package-solution.json
          grep -q 'SiteAssets/DispoPilotAssets' spfx-build/config/write-manifests.json
          grep -R '"SharePointFullPage"' spfx-build/src/webparts
          grep -R 'DP_Users' spfx-build/src/webparts

      - name: Build production bundle
        working-directory: spfx-build
        run: npx heft build --production

      - name: Package SharePoint solution
        working-directory: spfx-build
        run: npx heft package-solution --production

      - name: Collect SiteAssets and package
        run: |
          rm -rf artifact
          mkdir -p artifact/UPLOAD_TO_SITEASSETS
          mkdir -p artifact/UPLOAD_TO_APP_CATALOG

          PKG=$(find spfx-build/sharepoint/solution -maxdepth 1 -type f -name '*.sppkg' | head -n 1)
          test -n "$PKG"
          cp "$PKG" artifact/UPLOAD_TO_APP_CATALOG/DispoPilot_Online_V1.3_SPFX_FINAL.sppkg

          test -d spfx-build/temp/deploy
          cp -R spfx-build/temp/deploy/. artifact/UPLOAD_TO_SITEASSETS/

          test "$(find artifact/UPLOAD_TO_SITEASSETS -type f | wc -l)" -gt 0
          echo "=== Dateien fuer SiteAssets ==="
          find artifact/UPLOAD_TO_SITEASSETS -maxdepth 2 -type f -printf '%P\n' | sort
          echo "=== App-Paket ==="
          ls -lh artifact/UPLOAD_TO_APP_CATALOG/

          cat > artifact/REIHENFOLGE.txt <<'EOF'
          1. Alle Dateien aus UPLOAD_TO_SITEASSETS in:
             /sites/DispoPilot/SiteAssets/DispoPilotAssets
             hochladen.
          2. Danach DispoPilot_Online_V1.3_SPFX_FINAL.sppkg im App-Katalog ersetzen.
          3. Danach DispoPilot-Seite neu laden und externen Gast testen.

          Dies ist nur ein technischer Hosting-Reparaturbuild.
          Die DispoPilot-Funktionen und Rollenlogik wurden nicht geaendert.
          EOF

      - name: Upload repair artifact
        uses: actions/upload-artifact@v4
        with:
          name: DispoPilot-V1.3-SITEASSETS-REPAIR
          path: artifact/
          if-no-files-found: error
